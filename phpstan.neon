parameters:
    level: 4
    paths:
        - telegram_wordpress_bot.php
    # The WordPress connector relies on WP core symbols that PHPStan cannot see
    # without the WP stubs package, so it is excluded from static analysis here.
